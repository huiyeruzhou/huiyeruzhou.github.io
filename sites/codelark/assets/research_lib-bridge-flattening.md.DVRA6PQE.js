import{_ as s,o as a,c as n,a2 as o}from"./chunks/framework.BZO4qERu.js";const m=JSON.parse('{"title":"src 级自然聚合与 former bridge bucket 展平计划","description":"","frontmatter":{},"headers":[],"relativePath":"research/lib-bridge-flattening.md","filePath":"research/lib-bridge-flattening.md","lastUpdated":1780482865000}'),i={name:"research/lib-bridge-flattening.md"};function t(c,e,r,d,l,p){return a(),n("div",null,[...e[0]||(e[0]=[o(`<h1 id="src-级自然聚合与-former-bridge-bucket-展平计划" tabindex="-1"><code>src</code> 级自然聚合与 <code>former bridge bucket</code> 展平计划 <a class="header-anchor" href="#src-级自然聚合与-former-bridge-bucket-展平计划" aria-label="Permalink to &quot;\`src\` 级自然聚合与 \`former bridge bucket\` 展平计划&quot;">​</a></h1><p>生成时间：2026-06-03</p><h2 id="规划依据" tabindex="-1">规划依据 <a class="header-anchor" href="#规划依据" aria-label="Permalink to &quot;规划依据&quot;">​</a></h2><p>本计划使用历史 <code>work/analysis/analyze-imports.mjs</code> 作为采集工具，只取文件、行数、import 边、循环依赖等原始数据；不沿用旧脚本的 layer / cluster 聚合逻辑。</p><p>新的聚合逻辑来自当前代码的数据结构、文件名和实际 import 方向：</p><ul><li>产品持久数据实体：<code>BridgeSession</code>、<code>ChannelChat</code>、<code>ChannelDefaultTarget</code>、<code>BridgeMessage</code>、<code>PermissionLink</code>、<code>OutboundRef</code>、<code>AutoTask</code>、channel offset / dedup / audit。</li><li>Runtime 合同：<code>LLMProvider</code>、<code>StreamChatParams</code>、<code>SSEEvent</code>、<code>TokenUsage</code>、<code>BridgeMirrorRecord</code>、tool/task/context usage projection。</li><li>应用状态机：interactive turn、mirror subscription、mirror turn、stream feedback、session health、adapter lifecycle、auto task loop。</li><li>平台能力边界：Feishu、channel adapter capability、structured streaming card、callback / pin / preview / artifact delivery。</li></ul><p>核心判断：<code>former bridge bucket</code> 不是唯一特殊的大模块。它之所以显眼，是因为 domain contracts、runtime contracts、channel contracts 和 bridge daemon application 都堆在这里。真正目标应该是先建立 <code>src</code> 级 owner，再把 <code>former bridge bucket</code> 收缩为兼容层并删除。</p><h2 id="当前耦合事实" tabindex="-1">当前耦合事实 <a class="header-anchor" href="#当前耦合事实" aria-label="Permalink to &quot;当前耦合事实&quot;">​</a></h2><p>历史脚本在当前树上采集到：</p><ul><li><code>src/**/*.ts</code> 文件数：253。</li><li>本地 import / re-export 边数：1108。</li><li><code>former bridge bucket</code> 相关文件仍是全仓最密集区域之一。</li><li>多文件循环只有 <code>host.ts &lt;-&gt; types.ts</code>。</li></ul><p>按当前数据结构重新分组后的事实：</p><table tabindex="0"><thead><tr><th>当前区域</th><th style="text-align:right;">文件数 / 行数</th><th style="text-align:right;">入边 / 出边</th><th>说明</th></tr></thead><tbody><tr><td><code>former bridge bucket</code></td><td style="text-align:right;">110 / 34752</td><td style="text-align:right;">669 / 538</td><td>混合了 domain、runtime、channel、daemon app、platform adapter</td></tr><tr><td><code>src/ui</code></td><td style="text-align:right;">17 / 7211</td><td style="text-align:right;">33 / 58</td><td>Operator UI 直接读 config/store/bridge session 细节</td></tr><tr><td><code>src/codex</code></td><td style="text-align:right;">20 / 5401</td><td style="text-align:right;">107 / 57</td><td>Codex runtime/provider/session-index 反向依赖 bridge contracts 和 mirror types</td></tr><tr><td><code>src/claude</code></td><td style="text-align:right;">4 / 1309</td><td style="text-align:right;">24 / 13</td><td>Claude runtime 也依赖 bridge <code>LLMProvider/StreamChatParams</code></td></tr><tr><td><code>src/storage/json-store.ts</code> + migrations</td><td style="text-align:right;">2 / 1239</td><td style="text-align:right;">25 / 6</td><td>Persistence 直接 import bridge contracts 和 session-runtime</td></tr><tr><td><code>src/configuration/index.ts</code> + runtime options</td><td style="text-align:right;">2 / 1191</td><td style="text-align:right;">79 / 1</td><td>几乎所有模块都读全局 config 类型/默认值</td></tr><tr><td><code>src/local-service/manager.ts</code> + lock</td><td style="text-align:right;">2 / 1096</td><td style="text-align:right;">8 / 4</td><td>CLI/UI service control workflow 混合在顶层</td></tr></tbody></table><p>关键耦合方向：</p><ul><li><code>former bridge bucket -&gt; configuration</code>：25 条。</li><li><code>former bridge bucket -&gt; codex-runtime</code>：21 条。</li><li><code>codex-runtime -&gt; former bridge bucket</code>：17 条。</li><li><code>operator-ui -&gt; former bridge bucket</code>：11 条。</li><li><code>operator-ui -&gt; configuration</code>：10 条。</li><li><code>operator-ui -&gt; persistence</code>：7 条。</li><li><code>claude-runtime -&gt; former bridge bucket</code>：3 条。</li><li><code>persistence -&gt; former bridge bucket</code>：3 条。</li></ul><p>具体 import 证据：</p><ul><li>Codex / Claude provider 都直接 import <code>src/domain/session.ts / src/runtime/contracts.ts</code> 的 <code>LLMProvider</code> / <code>StreamChatParams</code>。</li><li>Codex session index parser import bridge <code>types</code>、<code>mirror-source</code>、<code>context-usage</code>、<code>tool-call-details</code>。</li><li>UI application 直接 import bridge session registry、display query、session runtime、transcript source。</li><li><code>store.ts</code> 直接 import bridge contracts 和 <code>session-runtime</code>。</li><li>已退役平台登录入口曾直接 import bridge adapter internals。</li></ul><p>结论：只规划 <code>former bridge bucket</code> 展平会低估问题。应先把 <code>src</code> 下共享的数据结构和 runtime/channel 合同提升出来，再处理 bridge daemon 内部状态机。</p><h2 id="目标结构" tabindex="-1">目标结构 <a class="header-anchor" href="#目标结构" aria-label="Permalink to &quot;目标结构&quot;">​</a></h2><p>建议目标结构：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>src/</span></span>
<span class="line"><span>  domain/</span></span>
<span class="line"><span>    session.ts</span></span>
<span class="line"><span>    channel.ts</span></span>
<span class="line"><span>    message.ts</span></span>
<span class="line"><span>    permission.ts</span></span>
<span class="line"><span>    automation.ts</span></span>
<span class="line"><span>    progress.ts</span></span>
<span class="line"><span>    audit.ts</span></span>
<span class="line"><span>  runtime/</span></span>
<span class="line"><span>    contracts.ts</span></span>
<span class="line"><span>    sse.ts</span></span>
<span class="line"><span>    permission-gateway.ts</span></span>
<span class="line"><span>    codex/</span></span>
<span class="line"><span>    claude/</span></span>
<span class="line"><span>    local-transcript/</span></span>
<span class="line"><span>  channels/</span></span>
<span class="line"><span>    contracts.ts</span></span>
<span class="line"><span>    adapter-runtime/</span></span>
<span class="line"><span>    delivery/</span></span>
<span class="line"><span>    feishu/</span></span>
<span class="line"><span>  bridge/</span></span>
<span class="line"><span>    host/</span></span>
<span class="line"><span>    turn/</span></span>
<span class="line"><span>    mirror/</span></span>
<span class="line"><span>    command/</span></span>
<span class="line"><span>    automation/</span></span>
<span class="line"><span>    health/</span></span>
<span class="line"><span>    diagnostics/</span></span>
<span class="line"><span>    tmux/</span></span>
<span class="line"><span>  storage/</span></span>
<span class="line"><span>    json-store.ts</span></span>
<span class="line"><span>    migrations.ts</span></span>
<span class="line"><span>    paths.ts</span></span>
<span class="line"><span>  configuration/</span></span>
<span class="line"><span>    index.ts</span></span>
<span class="line"><span>    runtime-options.ts</span></span>
<span class="line"><span>    channels.ts</span></span>
<span class="line"><span>  operator-ui/</span></span>
<span class="line"><span>    server.ts</span></span>
<span class="line"><span>    shell.ts</span></span>
<span class="line"><span>    assets.ts</span></span>
<span class="line"><span>    routes/</span></span>
<span class="line"><span>    application/</span></span>
<span class="line"><span>  local-service/</span></span>
<span class="line"><span>    bridge-process.ts</span></span>
<span class="line"><span>    ui-process.ts</span></span>
<span class="line"><span>    autostart.ts</span></span>
<span class="line"><span>    package-maintenance.ts</span></span>
<span class="line"><span>  entrypoints/</span></span>
<span class="line"><span>    daemon.ts</span></span>
<span class="line"><span>    cli.ts</span></span>
<span class="line"><span>  shared/</span></span>
<span class="line"><span>    logger.ts</span></span>
<span class="line"><span>    qrcode.d.ts</span></span></code></pre></div><p>迁移期可以保留现有 <code>src/codex</code>、<code>src/claude</code>、<code>src/ui</code> 等目录作为 facade，避免一次性大 churn；最终路径应表达 owner，而不是历史来源。</p><h2 id="全局-owner-逻辑" tabindex="-1">全局 owner 逻辑 <a class="header-anchor" href="#全局-owner-逻辑" aria-label="Permalink to &quot;全局 owner 逻辑&quot;">​</a></h2><h3 id="_1-domain" tabindex="-1">1. <code>domain</code> <a class="header-anchor" href="#_1-domain" aria-label="Permalink to &quot;1. \`domain\`&quot;">​</a></h3><p>Owner：产品持久数据模型，不依赖 runtime、UI、adapter 或 store 实现。</p><p>应聚合：</p><ul><li><code>BridgeSession</code>、<code>BridgeSession.runtime</code>、session health fields。</li><li><code>ChannelChat</code>、<code>ChannelDefaultTarget</code>、<code>ChannelAddress</code>。</li><li><code>BridgeMessage</code>、<code>InboundMessage</code>、<code>OutboundMessage</code>、rich card DTO。</li><li><code>PermissionLink</code>、permission callback storage DTO。</li><li><code>AutoTask</code>。</li><li><code>TaskProgressInfo</code>、<code>ToolCallInfo</code>、<code>ContextUsageInfo</code>、audit/dedup/outbound ref DTO。</li></ul><p>当前来源：</p><ul><li><code>src/domain/session.ts / src/runtime/contracts.ts</code></li><li><code>src/domain/index.ts</code></li><li><code>src/bridge/automation/auto-tasks.ts</code></li><li><code>src/shared/progress/context-usage.ts</code></li><li><code>src/shared/progress/tool-call-details.ts</code></li></ul><p>判断标准：如果某个类型需要被 UI、store、bridge app、runtime provider、channel adapter 共同理解，它不属于 bridge app，应进入 <code>domain</code>。</p><h3 id="_2-runtime" tabindex="-1">2. <code>runtime</code> <a class="header-anchor" href="#_2-runtime" aria-label="Permalink to &quot;2. \`runtime\`&quot;">​</a></h3><p>Owner：模型运行时合同和具体模型 runtime 实现。</p><p>应聚合：</p><ul><li><code>LLMProvider</code>、<code>StreamChatParams</code>、<code>SSEEvent</code>、<code>TokenUsage</code>。</li><li>Runtime-neutral JSONL mirror source contract：<code>MirrorJsonlSource</code>、<code>BridgeMirrorRecord</code>。</li><li>Codex provider、Codex pty/tmux provider、Codex routing provider。</li><li>Codex local session index / JSONL parser / mirror source。</li><li>Claude pty/sdk provider、Claude session JSONL reader。</li><li><code>permission-gateway.ts</code>、<code>sse-utils.ts</code>。</li></ul><p>目标路径：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>src/runtime/</span></span>
<span class="line"><span>  contracts.ts</span></span>
<span class="line"><span>  sse.ts</span></span>
<span class="line"><span>  permission-gateway.ts</span></span>
<span class="line"><span>  codex/</span></span>
<span class="line"><span>    provider.ts</span></span>
<span class="line"><span>    pty-provider.ts</span></span>
<span class="line"><span>    tmux-provider.ts</span></span>
<span class="line"><span>    routing-provider.ts</span></span>
<span class="line"><span>    session-index/</span></span>
<span class="line"><span>  claude/</span></span>
<span class="line"><span>    pty-provider.ts</span></span>
<span class="line"><span>    sdk-provider.ts</span></span>
<span class="line"><span>    session-jsonl.ts</span></span>
<span class="line"><span>    code-router.ts</span></span></code></pre></div><p>判断标准：如果逻辑负责启动 Codex/Claude、解析 runtime 输出、恢复 runtime identity，或者定义 runtime 调用合同，它属于 <code>runtime</code>。它不应 import bridge app 的 <code>host.ts</code>。</p><h3 id="_3-channels" tabindex="-1">3. <code>channels</code> <a class="header-anchor" href="#_3-channels" aria-label="Permalink to &quot;3. \`channels\`&quot;">​</a></h3><p>Owner：IM channel 合同、adapter runtime、平台实现、可靠 delivery。</p><p>应聚合：</p><ul><li>Channel adapter contract 与 capability interfaces。</li><li>Adapter registry / sync plan / adapter lifecycle runtime。</li><li>Delivery layer、feedback delivery、stream feedback、outbound artifacts、callback data。</li><li>Feishu adapter、Feishu markdown/card/media/setup notice。</li><li>Weixin adapter、Weixin API/media/ids/session guard/types。</li></ul><p>目标路径：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>src/channels/</span></span>
<span class="line"><span>  contracts.ts</span></span>
<span class="line"><span>  adapter-runtime/</span></span>
<span class="line"><span>    registry.ts</span></span>
<span class="line"><span>    runtime.ts</span></span>
<span class="line"><span>    sync-plan.ts</span></span>
<span class="line"><span>    capabilities/</span></span>
<span class="line"><span>  delivery/</span></span>
<span class="line"><span>    deliver.ts</span></span>
<span class="line"><span>    feedback.ts</span></span>
<span class="line"><span>    stream-feedback.ts</span></span>
<span class="line"><span>    artifacts.ts</span></span>
<span class="line"><span>    callbacks.ts</span></span>
<span class="line"><span>  feishu/</span></span></code></pre></div><p>判断标准：如果逻辑依赖平台 API、平台消息结构、平台卡片能力，或者负责把 domain message 送到 IM，它属于 <code>channels</code>。Bridge app 只通过 <code>channels/contracts</code> 和 <code>channels/delivery</code> 调用它。</p><h3 id="_4-bridge" tabindex="-1">4. <code>bridge</code> <a class="header-anchor" href="#_4-bridge" aria-label="Permalink to &quot;4. \`bridge\`&quot;">​</a></h3><p>Owner：Codex-to-IM daemon application workflow。它组合 domain、runtime、channels、storage、configuration，但不拥有这些层的数据结构。</p><p>应聚合：</p><ul><li>Host lifecycle：start/stop/status/startup notice/inbound dispatch。</li><li>Interactive turn 状态机。</li><li>Mirror 状态机。</li><li>Slash command use-case 和 presentation。</li><li>Automation runtime。</li><li>Session health runtime。</li><li>Tmux remote-control command/runtime。</li><li>Doctor/diagnostics use-case。</li></ul><p>目标路径：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>src/bridge/</span></span>
<span class="line"><span>  host/</span></span>
<span class="line"><span>    manager.ts</span></span>
<span class="line"><span>    context.ts</span></span>
<span class="line"><span>    inbound-dispatch.ts</span></span>
<span class="line"><span>    startup-notice.ts</span></span>
<span class="line"><span>  turn/</span></span>
<span class="line"><span>  mirror/</span></span>
<span class="line"><span>  command/</span></span>
<span class="line"><span>  automation/</span></span>
<span class="line"><span>  health/</span></span>
<span class="line"><span>  diagnostics/</span></span>
<span class="line"><span>  tmux/</span></span></code></pre></div><p>判断标准：如果对象生命周期是 daemon 运行时的一段 workflow，它属于 <code>bridge</code>。如果它是跨模块数据合同，就不属于 <code>bridge</code>。</p><h3 id="_5-storage" tabindex="-1">5. <code>storage</code> <a class="header-anchor" href="#_5-storage" aria-label="Permalink to &quot;5. \`storage\`&quot;">​</a></h3><p>Owner：本地 JSON 存储、数据迁移、文件路径和 atomic write。</p><p>应聚合：</p><ul><li><code>store.ts</code></li><li><code>storage-migrations.ts</code></li><li>CTI data path helper，但不拥有 config defaults。</li></ul><p>目标是让 store 依赖 <code>domain</code> contracts，而不是 <code>src/domain/session.ts / src/runtime/contracts.ts</code> / <code>types.ts</code> / <code>session-runtime.ts</code>。</p><h3 id="_6-configuration" tabindex="-1">6. <code>configuration</code> <a class="header-anchor" href="#_6-configuration" aria-label="Permalink to &quot;6. \`configuration\`&quot;">​</a></h3><p>Owner：<code>config.v2.json</code>、env mapping、runtime/channel config schema、runtime option parsing。</p><p>应聚合：</p><ul><li><code>config.ts</code></li><li><code>runtime-options.ts</code></li><li>channel instance config helpers。</li></ul><p>目标是让 UI、bridge app、runtime provider 读取明确的 config query，而不是每层直接 import 大 <code>config.ts</code> 并复制解释规则。</p><h3 id="_7-operator-ui" tabindex="-1">7. <code>operator-ui</code> <a class="header-anchor" href="#_7-operator-ui" aria-label="Permalink to &quot;7. \`operator-ui\`&quot;">​</a></h3><p>Owner：本地 Web 工作台的 operator workflows。</p><p>当前 <code>src/ui</code> 已经部分聚合，但仍直接 import store/config/bridge internals。目标：</p><ul><li>routes 只做 HTTP decode/encode。</li><li>application 层调用 <code>storage</code>/<code>configuration</code>/<code>bridge</code>/<code>runtime</code> 的 query/service facade。</li><li>不直接 import bridge session registry internals。</li></ul><p>最终可从 <code>src/ui</code> 改名为 <code>src/operator-ui</code>；也可保留路径但按上述 owner 收紧。</p><h3 id="_8-local-service" tabindex="-1">8. <code>local-service</code> <a class="header-anchor" href="#_8-local-service" aria-label="Permalink to &quot;8. \`local-service\`&quot;">​</a></h3><p>Owner：本地进程、开机自启、UI server process、包维护、Codex integration 安装。</p><p>当前 <code>service-manager.ts</code> 是 catch-all。目标拆分：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>src/local-service/</span></span>
<span class="line"><span>  bridge-process.ts</span></span>
<span class="line"><span>  ui-process.ts</span></span>
<span class="line"><span>  autostart.ts</span></span>
<span class="line"><span>  package-maintenance.ts</span></span>
<span class="line"><span>  codex-integration.ts</span></span></code></pre></div><p>CLI 和 UI service route 调用这些 workflow，不直接拼接平台脚本细节。</p><h3 id="_9-entrypoints" tabindex="-1">9. <code>entrypoints</code> <a class="header-anchor" href="#_9-entrypoints" aria-label="Permalink to &quot;9. \`entrypoints\`&quot;">​</a></h3><p>Owner：进程入口和依赖装配。</p><p>应聚合：</p><ul><li><code>main.ts</code> -&gt; <code>entrypoints/daemon.ts</code></li><li><code>cli.ts</code> -&gt; <code>entrypoints/cli.ts</code></li></ul><p>它们只做 wiring，不拥有业务规则。</p><h2 id="former-bridge-bucket-的新定位" tabindex="-1"><code>former bridge bucket</code> 的新定位 <a class="header-anchor" href="#former-bridge-bucket-的新定位" aria-label="Permalink to &quot;\`former bridge bucket\` 的新定位&quot;">​</a></h2><p>在全局 owner 建立后，<code>former bridge bucket</code> 不应直接迁成 <code>src/bridge</code>。更准确的拆法是：</p><ul><li><code>host.ts</code> / <code>types.ts</code> 的跨模块合同先进入 <code>domain</code>、<code>runtime/contracts</code>、<code>channels/contracts</code>。</li><li><code>channel-adapter.ts</code> 的平台无关 contract 进入 <code>channels/contracts</code>，具体 adapter 进入 <code>channels/feishu</code>。</li><li><code>mirror-source.ts</code> 的 runtime-neutral source contract 进入 <code>runtime/contracts</code> 或 <code>runtime/local-transcript</code>，mirror 状态机进入 <code>bridge/mirror</code>。</li><li><code>session-runtime.ts</code> 这种操作 <code>BridgeSession.runtime</code> 的 accessor 进入 <code>domain/session-runtime.ts</code> 或 <code>bridge/session</code>，取决于是否仍依赖 daemon 状态。当前更偏 <code>domain</code> helper。</li><li><code>bridge-manager.ts</code>、interactive turn、mirror runtime、command、auto task、health 等 workflow 进入 <code>src/bridge</code>。</li></ul><p>这比“把 <code>former bridge bucket</code> 整体搬到 <code>src/bridge</code>”更符合当前耦合事实。</p><h2 id="迁移阶段" tabindex="-1">迁移阶段 <a class="header-anchor" href="#迁移阶段" aria-label="Permalink to &quot;迁移阶段&quot;">​</a></h2><h3 id="阶段-1-建立全局-contracts-不再让其他模块-import-former-bridge-bucket-host-ts" tabindex="-1">阶段 1：建立全局 contracts，不再让其他模块 import <code>former bridge bucket/host.ts</code> <a class="header-anchor" href="#阶段-1-建立全局-contracts-不再让其他模块-import-former-bridge-bucket-host-ts" aria-label="Permalink to &quot;阶段 1：建立全局 contracts，不再让其他模块 import \`former bridge bucket/host.ts\`&quot;">​</a></h3><p>动作：</p><ul><li>新增 <code>src/domain/*</code>，承接 <code>BridgeSession</code>、<code>ChannelChat</code>、message、permission、auto task、progress/tool 等 DTO。</li><li>新增 <code>src/runtime/contracts.ts</code>，承接 <code>LLMProvider</code>、<code>StreamChatParams</code>、<code>SSEEvent</code>、<code>TokenUsage</code>、<code>MirrorJsonlSource</code>、<code>BridgeMirrorRecord</code>。</li><li>新增 <code>src/channels/contracts.ts</code>，承接 adapter base contract 与 channel capability interface。</li><li>让 <code>src/domain/session.ts / src/runtime/contracts.ts</code>、<code>types.ts</code>、<code>mirror-source.ts</code> 暂时 re-export 新 contracts。</li><li>迁移 Codex/Claude provider、store、UI application 的类型 import 到新 contracts。</li></ul><p>验证：</p><ul><li><code>npm run typecheck</code></li><li>复跑依赖脚本，目标是 <code>codex-runtime -&gt; legacy-bridge</code>、<code>claude-runtime -&gt; legacy-bridge</code>、<code>persistence -&gt; legacy-bridge</code> 明显下降。</li><li>多文件循环 <code>host.ts &lt;-&gt; types.ts</code> 消失。</li></ul><h3 id="阶段-2-拆-storage-configuration-owner" tabindex="-1">阶段 2：拆 storage / configuration owner <a class="header-anchor" href="#阶段-2-拆-storage-configuration-owner" aria-label="Permalink to &quot;阶段 2：拆 storage / configuration owner&quot;">​</a></h3><p>动作：</p><ul><li><code>store.ts</code> 迁到 <code>src/storage/json-store.ts</code>，只依赖 <code>domain</code> contracts。</li><li><code>storage-migrations.ts</code> 迁到 <code>src/storage/migrations.ts</code>。</li><li><code>config.ts</code> 拆出 <code>configuration/index.ts</code>、<code>configuration/channels.ts</code>、<code>configuration/runtime-defaults.ts</code>。</li><li><code>runtime-options.ts</code> 迁到 <code>configuration/runtime-options.ts</code> 或 <code>runtime/options.ts</code>，按是否纯 Codex runtime 决定。</li></ul><p>验证：</p><ul><li>store/config/storage migration 测试。</li><li>UI config routes 测试。</li><li><code>npm run typecheck</code>。</li></ul><h3 id="阶段-3-runtime-provider-归位" tabindex="-1">阶段 3：runtime provider 归位 <a class="header-anchor" href="#阶段-3-runtime-provider-归位" aria-label="Permalink to &quot;阶段 3：runtime provider 归位&quot;">​</a></h3><p>动作：</p><ul><li><code>src/runtime/codex/*</code> 迁到 <code>src/runtime/codex/*</code>，保留 <code>src/runtime/codex/*</code> facade。</li><li><code>src/runtime/claude/*</code> 迁到 <code>src/runtime/claude/*</code>，保留 <code>src/runtime/claude/*</code> facade。</li><li>Codex / Claude 不再 import bridge app；只 import <code>domain</code>、<code>runtime/contracts</code>、<code>configuration</code>、<code>shared</code>。</li><li>Codex session-index 中的 tool/context projection 若跨 runtime 可复用，放入 <code>domain/progress.ts</code> 或 <code>runtime/transcript-projection.ts</code>。</li></ul><p>验证：</p><ul><li>Codex provider / routing / session-index / mirror 测试。</li><li>Claude provider / session-jsonl 测试。</li><li><code>npm test</code> 中 runtime 相关套件。</li></ul><h3 id="阶段-4-channels-归位" tabindex="-1">阶段 4：channels 归位 <a class="header-anchor" href="#阶段-4-channels-归位" aria-label="Permalink to &quot;阶段 4：channels 归位&quot;">​</a></h3><p>动作：</p><ul><li><code>src/channels/feishu/adapter.ts</code> 拆迁到 <code>src/channels/feishu</code>。</li><li>已退役平台 adapter、登录和账号存储入口直接删除，不再迁入新 owner。</li><li><code>delivery-layer.ts</code>、<code>feedback-delivery.ts</code>、<code>stream-feedback-controller.ts</code> 迁到 <code>channels/delivery</code>。</li><li>Adapter runtime / sync plan 迁到 <code>channels/adapter-runtime</code>。</li></ul><p>验证：</p><ul><li>Feishu adapter / markdown / card e2e tests。</li><li>Weixin adapter / API / media / login / store tests。</li><li>delivery pipeline / stream feedback tests。</li></ul><h3 id="阶段-5-bridge-daemon-workflows-归位" tabindex="-1">阶段 5：bridge daemon workflows 归位 <a class="header-anchor" href="#阶段-5-bridge-daemon-workflows-归位" aria-label="Permalink to &quot;阶段 5：bridge daemon workflows 归位&quot;">​</a></h3><p>动作：</p><ul><li><code>bridge-manager.ts</code> 收缩为 <code>src/bridge/host/manager.ts</code>。</li><li><code>interactive-turn/*</code> + <code>turns/*</code> 进入 <code>src/bridge/turn</code>。</li><li><code>mirror-*</code> 的状态机进入 <code>src/bridge/mirror</code>，但 mirror source contract 留在 <code>runtime</code>。</li><li><code>command/*</code> 进入 <code>src/bridge/command</code>，只调用 owner facade。</li><li><code>auto-tasks.ts</code> 中数据 contract 留在 <code>domain/automation.ts</code>，runtime loop 进入 <code>src/bridge/automation</code>。</li><li><code>session-health-*</code> 进入 <code>src/bridge/health</code>。</li></ul><p>验证：</p><ul><li>bridge manager / command / interactive turn / mirror / health / automation 相关测试。</li><li>完整 <code>npm test</code>。</li></ul><h3 id="阶段-6-operator-ui-和-local-service-收口" tabindex="-1">阶段 6：operator UI 和 local service 收口 <a class="header-anchor" href="#阶段-6-operator-ui-和-local-service-收口" aria-label="Permalink to &quot;阶段 6：operator UI 和 local service 收口&quot;">​</a></h3><p>动作：</p><ul><li>UI application 不再直接 import bridge internals；改用 domain DTO + storage/config/runtime/channel query facade。</li><li><code>service-manager.ts</code> 拆到 <code>src/local-service/*</code>。</li><li><code>main.ts</code> / <code>cli.ts</code> 迁到 <code>src/entrypoints/*</code>，只做 wiring。</li></ul><p>验证：</p><ul><li>UI route/application tests。</li><li>service-manager tests。</li><li>build / typecheck / full test。</li></ul><h3 id="阶段-7-删除兼容层" tabindex="-1">阶段 7：删除兼容层 <a class="header-anchor" href="#阶段-7-删除兼容层" aria-label="Permalink to &quot;阶段 7：删除兼容层&quot;">​</a></h3><p>动作：</p><ul><li>删除 <code>former bridge bucket</code> re-export facade。</li><li>删除 <code>src/codex</code> / <code>src/claude</code> / <code>src/ui</code> 等临时 facade，或明确保留为 public alias。</li><li>扫描所有旧路径。</li></ul><p>验证：</p><ul><li>扫描源码、文档、脚本、schema 中的旧 bridge bucket 和旧 runtime facade import 字符串。</li><li><code>npm run typecheck</code></li><li><code>npm run build</code></li><li><code>npm test</code></li></ul><h2 id="推荐第一刀" tabindex="-1">推荐第一刀 <a class="header-anchor" href="#推荐第一刀" aria-label="Permalink to &quot;推荐第一刀&quot;">​</a></h2><p>第一刀不建议再以 <code>bridge/session/contracts</code> 为中心。应做：</p><ol><li><code>domain</code> contracts：session、channel、message、permission、automation、progress/tool。</li><li><code>runtime/contracts</code>：LLM provider、stream params、SSE、mirror source/record。</li><li><code>channels/contracts</code>：adapter base contract 和 capability interfaces。</li><li><code>src/domain/session.ts / src/runtime/contracts.ts</code>、<code>types.ts</code>、<code>mirror-source.ts</code> 改为兼容 re-export。</li><li>迁移 Codex/Claude provider、store、UI application 的类型 import。</li></ol><p>理由：这几类模块现在都通过 <code>former bridge bucket</code> 互相耦合。先抽全局 contracts，才能让后续 <code>former bridge bucket</code> 展平不变成“把 bridge 换个名字继续当公共桶”。</p><h2 id="不做什么" tabindex="-1">不做什么 <a class="header-anchor" href="#不做什么" aria-label="Permalink to &quot;不做什么&quot;">​</a></h2><ul><li>不沿用历史脚本的聚合分类作为目标边界。</li><li>不把 <code>bridge/core</code>、<code>cluster-01</code>、<code>cluster-02</code> 这类审计标签变成目录名。</li><li>不把 <code>former bridge bucket</code> 整体搬到 <code>src/bridge</code> 后继续承载所有共享类型。</li><li>不让 <code>runtime</code> provider import bridge daemon application。</li><li>不让 <code>storage</code> import bridge app contracts。</li><li>不让 operator UI 直接读取 bridge session registry internals。</li><li>不新增大而空的 <code>composition</code> / <code>factory</code> / <code>deps</code> 层。</li><li>不把 <code>bridge-manager.ts</code> 的复杂度搬到另一个大 manager。</li></ul>`,121)])])}const g=s(i,[["render",t]]);export{m as __pageData,g as default};
