import{_ as t,o as s,c as d,a2 as a}from"./chunks/framework.BZO4qERu.js";const u=JSON.parse('{"title":"测试代码结构重建计划","description":"","frontmatter":{},"headers":[],"relativePath":"testing/rebuild-structure.md","filePath":"testing/rebuild-structure.md","lastUpdated":1780509827000}'),o={name:"testing/rebuild-structure.md"};function n(r,e,c,i,p,l){return s(),d("div",null,[...e[0]||(e[0]=[a(`<h1 id="测试代码结构重建计划" tabindex="-1">测试代码结构重建计划 <a class="header-anchor" href="#测试代码结构重建计划" aria-label="Permalink to &quot;测试代码结构重建计划&quot;">​</a></h1><p>本文记录测试目录和测试职责。源码已按 owner 展开后，测试也按 owner、验证强度和公共工具边界分层，避免继续把所有 suite 平铺在 <code>src/__tests__</code>。</p><h2 id="重建目标" tabindex="-1">重建目标 <a class="header-anchor" href="#重建目标" aria-label="Permalink to &quot;重建目标&quot;">​</a></h2><p>测试结构应同时回答三个问题：</p><ol><li>这个测试保护哪个源码 owner。</li><li>这个测试使用哪一级验证强度。</li><li>这个测试是否可以作为用户故事或发布 gate 的证据。</li></ol><p>新结构不追求把每个源码文件逐一镜像成一个测试文件。功能点非常单一、没有跨模块状态的逻辑，只保留单测；一个 owner 内有多个协作点时，配一个本地打包 workflow/e2e；跨一大组 owner、依赖 provider 启动或 CLI 协议时，配真实可执行文件加 proxy 的本地 e2e；跨多个模块、全局状态切换且用户可见的完整故事，交给真实飞书 E2E。</p><h2 id="目标目录" tabindex="-1">目标目录 <a class="header-anchor" href="#目标目录" aria-label="Permalink to &quot;目标目录&quot;">​</a></h2><p>当前结构：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>src/__tests__/</span></span>
<span class="line"><span>  setup/</span></span>
<span class="line"><span>    test-setup.ts</span></span>
<span class="line"><span>  helpers/</span></span>
<span class="line"><span>    bridge/</span></span>
<span class="line"><span>      test-bridge-utils.ts</span></span>
<span class="line"><span>    channels/</span></span>
<span class="line"><span>    runtime/</span></span>
<span class="line"><span>      real-codex-e2e-utils.ts</span></span>
<span class="line"><span>    storage/</span></span>
<span class="line"><span>    assertions/</span></span>
<span class="line"><span>  unit/</span></span>
<span class="line"><span>    domain/</span></span>
<span class="line"><span>    shared/</span></span>
<span class="line"><span>    configuration/</span></span>
<span class="line"><span>    storage/</span></span>
<span class="line"><span>    runtime/</span></span>
<span class="line"><span>      codex/</span></span>
<span class="line"><span>      claude/</span></span>
<span class="line"><span>    channels/</span></span>
<span class="line"><span>      delivery/</span></span>
<span class="line"><span>      feishu/</span></span>
<span class="line"><span>    bridge/</span></span>
<span class="line"><span>      command/</span></span>
<span class="line"><span>      session/</span></span>
<span class="line"><span>      mirror/</span></span>
<span class="line"><span>      turn/</span></span>
<span class="line"><span>      health/</span></span>
<span class="line"><span>      diagnostics/</span></span>
<span class="line"><span>      tmux/</span></span>
<span class="line"><span>    operator-ui/</span></span>
<span class="line"><span>    local-service/</span></span>
<span class="line"><span>  workflow/</span></span>
<span class="line"><span>    bridge/</span></span>
<span class="line"><span>    channels/</span></span>
<span class="line"><span>    local-service/</span></span>
<span class="line"><span>    operator-ui/</span></span>
<span class="line"><span>    runtime/</span></span>
<span class="line"><span>    storage/</span></span>
<span class="line"><span>  e2e/</span></span>
<span class="line"><span>    mock-app/</span></span>
<span class="line"><span>      bridge/</span></span>
<span class="line"><span>      channels/</span></span>
<span class="line"><span>    local-process/</span></span>
<span class="line"><span>      codex/</span></span>
<span class="line"><span>      claude/</span></span>
<span class="line"><span>      tmux/</span></span>
<span class="line"><span>  harness/</span></span>
<span class="line"><span>    real-feishu/</span></span>
<span class="line"><span>scripts/</span></span>
<span class="line"><span>  real-feishu-e2e.ts</span></span></code></pre></div><p><code>src/__tests__/unit</code> 与源码 owner 对齐。<code>workflow</code> 用 fake adapter/provider/store 打包一个 owner 内或相邻 owner 的协作链路。<code>e2e/mock-app</code> 是本地可重复的应用级 e2e，可以使用 mock provider 和 mock channel。<code>e2e/local-process</code> 必须启动真实可执行文件或真实进程边界，并通过本地 proxy/fake backend 控制模型结果。<code>harness/real-feishu</code> 测真实飞书 harness 自身，真实飞书长流程仍由 <code>scripts/real-feishu-e2e.ts</code> 执行。</p><p><code>fixtures/</code> 只放可被多个测试层级复用的静态输入数据，例如 config JSON、Codex/Claude JSONL、飞书 payload 和 transcript。只被单个 suite 使用的数据应放在该 suite 旁边，避免全局 fixtures 变成第二个无 owner 的杂物目录。动态生成的 store、home、proxy 和 adapter 不属于 fixtures，应放在 <code>setup/</code> 或 <code>helpers/</code>。</p><p>目前尚未引入全局 <code>fixtures/</code> 目录；现有静态输入仍就近保留在对应 suite 中。只有当同一份静态输入跨多个层级复用时才新增 <code>fixtures/</code>。</p><h2 id="强度分层" tabindex="-1">强度分层 <a class="header-anchor" href="#强度分层" aria-label="Permalink to &quot;强度分层&quot;">​</a></h2><table tabindex="0"><thead><tr><th>层级</th><th>目录</th><th>依赖</th><th>应证明的事</th><th>不应承担的事</th></tr></thead><tbody><tr><td>单测</td><td><code>unit/&lt;owner&gt;/</code></td><td>纯函数、小对象、临时文件</td><td>单一解析、转换、状态 reducer、presentation、schema、路径规则正确</td><td>不证明用户故事，不 mock 一整条聊天流程</td></tr><tr><td>本地 workflow</td><td><code>workflow/&lt;owner-or-slice&gt;/</code></td><td>fake adapter/provider/store</td><td>一个 owner 或相邻 owner 组合后能完成一组稳定动作</td><td>不启动真实 Codex/Claude，不依赖飞书</td></tr><tr><td>本地 mock e2e</td><td><code>e2e/mock-app/&lt;slice&gt;/</code></td><td>bridge host + fake channel + fake provider</td><td>daemon 级编排、绑定、状态持久化、交付动作能闭环</td><td>不证明 provider 可执行文件或飞书客户端契约</td></tr><tr><td>本地真实进程 e2e</td><td><code>e2e/local-process/&lt;runtime&gt;/</code></td><td>真实 <code>codex</code>/<code>claude</code>/<code>tmux</code> 可执行文件 + 本地 proxy/fake backend</td><td>provider 启动、CLI 参数、JSONL/mirror、清理和 provider 输出路径可信</td><td>不证明飞书事件投递、<code>reply_to</code> 或真实卡片渲染</td></tr><tr><td>真实飞书 E2E</td><td><code>scripts/real-feishu-e2e.ts</code> 场景</td><td>真实 lark-cli、真实群、真实 bridge、可控 provider 后端</td><td>完整用户故事、跨模块全局状态、真实飞书消息/卡片/文件/清理 gate</td><td>不逐条镜像单测和本地 workflow</td></tr></tbody></table><p>判断规则：</p><ul><li>一个函数或 formatter 的输入输出足够描述风险时，只写单测。</li><li>一个命令组、session registry、delivery pipeline、mirror controller 等包含多个内部组件时，保留同组 workflow。</li><li>一个 runtime/provider 功能涉及真实 pty、tmux、CLI bootstrap、JSONL 文件或 proxy 协议时，必须有 <code>e2e/local-process</code>。</li><li>用户从飞书发消息开始，经历 runtime/provider 切换、跨群绑定、历史、表单、附件、权限或清理时，只能由真实飞书 E2E 作为最终证据。</li></ul><h2 id="公共工具" tabindex="-1">公共工具 <a class="header-anchor" href="#公共工具" aria-label="Permalink to &quot;公共工具&quot;">​</a></h2><p>公共工具已从平铺测试根目录移出。第一阶段保持 helper API 稳定，只改变所有权位置；后续拆分时仍按下表边界收敛，避免重新形成万能 helper。</p><table tabindex="0"><thead><tr><th>目标位置</th><th>当前文件/内容</th><th>依赖边界</th></tr></thead><tbody><tr><td><code>setup/test-setup.ts</code></td><td>全局测试环境默认值、隔离 home guard 的共享 setup；未来可继续拆成 <code>isolated-home.ts</code>、<code>codex-home.ts</code></td><td>只能依赖 Node 标准库和最底层配置常量</td></tr><tr><td><code>helpers/storage/</code></td><td>临时 store、迁移输入、JSONL 写入</td><td>可依赖 <code>storage</code>、<code>configuration</code>、<code>domain</code></td></tr><tr><td><code>helpers/bridge/test-bridge-utils.ts</code></td><td>fake LLM、fake lifecycle、bridge context 初始化、turn/session 断言、Codex JSONL 写入</td><td>可依赖 <code>bridge</code>、<code>runtime/contracts</code>、<code>domain</code></td></tr><tr><td><code>helpers/channels/</code></td><td>recording adapter、fake Feishu payload、delivery 断言</td><td>可依赖 <code>channels/contracts</code>、<code>domain</code></td></tr><tr><td><code>helpers/runtime/real-codex-e2e-utils.ts</code></td><td>local responses proxy、fake Codex/Claude executable、pty/tmux 清理</td><td>可依赖 <code>runtime</code>，不反向依赖 <code>bridge</code>，除非测试目标是 app e2e</td></tr><tr><td><code>helpers/assertions/</code></td><td>文本、card、file、stream snapshot 断言</td><td>只放跨 owner 复用的断言，不放业务流程</td></tr></tbody></table><p>公共工具必须比测试目标更低层或同层。<code>unit/domain</code> 不应 import <code>helpers/bridge</code>；<code>unit/shared</code> 不应 import provider helper；<code>e2e/local-process</code> 可以使用 <code>helpers/runtime</code> 和 <code>setup</code>，但不应绕过真实 provider 启动路径。</p><h2 id="owner-对应关系" tabindex="-1">Owner 对应关系 <a class="header-anchor" href="#owner-对应关系" aria-label="Permalink to &quot;Owner 对应关系&quot;">​</a></h2><table tabindex="0"><thead><tr><th>源码 owner</th><th>单测重点</th><th>打包 workflow/e2e</th><th>真实可执行文件 + proxy</th><th>真实飞书用户故事</th></tr></thead><tbody><tr><td><code>domain</code></td><td>session/channel/message/permission DTO、runtime config 默认字段、工作目录解析</td><td>不需要固定 workflow，除非 domain 聚合规则跨多个 DTO</td><td>不需要</td><td>不需要</td></tr><tr><td><code>shared</code></td><td>Markdown fence、plain text、logger、progress projection、secret masking</td><td>Markdown/card projection 可在 channels workflow 复用</td><td>不需要</td><td>Markdown 真实渲染场景</td></tr><tr><td><code>configuration</code></td><td>config merge、runtime options、schema、secret masking</td><td>config + UI route 或 config + command 设置 workflow</td><td>不需要</td><td><code>/set</code>、runtime/provider/model/mode 设置场景</td></tr><tr><td><code>storage</code></td><td>JSON store round-trip、atomic write、migration</td><td>startup migration workflow、channel binding migration script</td><td>不需要</td><td>只在历史、绑定和清理故事中间接覆盖</td></tr><tr><td><code>channels</code></td><td>adapter sync plan、delivery plan、Feishu payload 转换、Markdown/card builder</td><td>fake adapter delivery pipeline、Feishu card mock e2e</td><td>不需要</td><td><code>reply_to</code>、真实卡片、文件、表单 callback</td></tr><tr><td><code>bridge/command</code></td><td>slash parse、command presentation、callback data、aliases</td><td>命令组 workflow，例如 session 命令、runtime 设置、auto、history</td><td>不需要</td><td><code>command-state</code>、<code>session-management</code>、<code>history-suite</code></td></tr><tr><td><code>bridge/session</code></td><td>registry、binding、display query、health query</td><td>chat/session binding workflow</td><td>不需要</td><td><code>/new</code>、<code>/cd</code>、<code>/current</code>、跨群隔离</td></tr><tr><td><code>bridge/turn</code></td><td>classifier、coordinator、stream state、final response plan</td><td>interactive turn workflow，使用 fake provider 和 fake adapter</td><td>SDK conversation 可由 mock-app e2e 覆盖</td><td>基础对话 suite、长流程 provider 切换</td></tr><tr><td><code>bridge/mirror</code></td><td>cursor、delivery plan、subscription state、reconcile</td><td>mirror runtime + feedback workflow</td><td>Codex/Claude JSONL mirror 进入 local-process e2e</td><td>pty/tmux provider 的 mirror 输出和去重</td></tr><tr><td><code>bridge/automation</code></td><td>auto task store、调度输入输出</td><td><code>/auto</code> 命令 workflow</td><td>不需要</td><td>auto 创建/列表/删除和卡片回调</td></tr><tr><td><code>bridge/health</code>、<code>bridge/diagnostics</code></td><td>doctor prompt、health status、dump range</td><td>startup/doctor workflow</td><td>不需要</td><td>只有诊断命令用户故事需要</td></tr><tr><td><code>bridge/tmux</code></td><td>tmux command parser、screen limit、routing</td><td>tmux command workflow</td><td>tmux provider local-process e2e</td><td>tmux provider 路径和清理 gate</td></tr><tr><td><code>runtime/codex</code></td><td>SSE decoder、model cache、CLI path、session index parser、routing provider</td><td>Codex provider mock stream workflow</td><td>codex pty/tmux/SDK proxy e2e</td><td>codex-sdk/codex-pty/codex-tmux 代表用户故事</td></tr><tr><td><code>runtime/claude</code></td><td>Claude JSONL parser、SDK helper、pty setup</td><td>Claude provider mock stream workflow</td><td>claude pty/sdk proxy e2e</td><td>claude-pty/claude-sdk 代表用户故事</td></tr><tr><td><code>operator-ui</code></td><td>route handler、application query/command、history projection</td><td>UI route workflow with fake request/config/store</td><td>不需要</td><td>仅当 UI 改动影响 IM 行为时补飞书</td></tr><tr><td><code>local-service</code>、<code>entrypoints</code></td><td>service lock、hot update dry run、CLI 参数</td><td>service manager workflow</td><td>必要时启动 detached dry-run 子进程</td><td>不在真实飞书默认矩阵中覆盖</td></tr></tbody></table><h2 id="bridge-command-拆分标注" tabindex="-1"><code>bridge/command</code> 拆分标注 <a class="header-anchor" href="#bridge-command-拆分标注" aria-label="Permalink to &quot;\`bridge/command\` 拆分标注&quot;">​</a></h2><p><code>bridge/command</code> 需要源码重构和测试重构同步推进。当前 <code>session-thread.ts</code>、<code>runtime-settings.ts</code>、<code>tmux.ts</code>、<code>dispatch.ts</code> 等文件同时承担命令识别、参数解析、权限/状态读取、业务动作、presentation、delivery 后处理和真实副作用编排。重建时先按现有自然功能块拆出最小必要边界，再让测试目录跟随这些边界；不要为了目录整齐把每条命令都拆成独立目录。</p><p>建议源码目标：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>src/bridge/command/</span></span>
<span class="line"><span>  dispatch.ts</span></span>
<span class="line"><span>  result.ts</span></span>
<span class="line"><span>  aliases.ts</span></span>
<span class="line"><span>  callbacks.ts</span></span>
<span class="line"><span>  clear-confirmations.ts</span></span>
<span class="line"><span>  errors.ts</span></span>
<span class="line"><span>  presentation/</span></span>
<span class="line"><span>    help.ts</span></span>
<span class="line"><span>    status.ts</span></span>
<span class="line"><span>    thread-list.ts</span></span>
<span class="line"><span>    history.ts</span></span>
<span class="line"><span>    diagnostics.ts</span></span>
<span class="line"><span>    auto.ts</span></span>
<span class="line"><span>  commands/</span></span>
<span class="line"><span>    session-thread.ts 或 session/</span></span>
<span class="line"><span>    runtime-settings.ts 或 runtime-settings/</span></span>
<span class="line"><span>    diagnostics.ts</span></span>
<span class="line"><span>    auto.ts</span></span>
<span class="line"><span>    tmux.ts 或 terminal/tmux.ts</span></span>
<span class="line"><span>    pty.ts 或 terminal/pty.ts</span></span>
<span class="line"><span>    shell.ts 或 terminal/shell.ts</span></span>
<span class="line"><span>    control.ts</span></span>
<span class="line"><span>    hot-update.ts</span></span>
<span class="line"><span>    global-settings.ts</span></span>
<span class="line"><span>    require-at.ts</span></span></code></pre></div><p><code>dispatch.ts</code> 是命令入口和交付编排，<code>result.ts</code> 只定义命令处理器返回给 dispatch 的结构化结果。<code>aliases.ts</code>、<code>callbacks.ts</code>、<code>clear-confirmations.ts</code>、<code>errors.ts</code> 这类小而稳定的文件可以留在根部，除非后续确实出现多个文件共享同一职责。<code>commands/</code> 也不按每个 slash command 建目录，而是按修改半径和上下文聚合：session-thread、runtime settings、diagnostics、auto、tmux/pty/shell、control、hot update、global settings、require-at。命令组如果仍能用一个文件表达清楚，就保持单文件；只有当该组已经拆出 parser、use-case、side-effect adapter 等多个低层文件时，才升级成目录。</p><p>对应测试分层：</p><table tabindex="0"><thead><tr><th>层级</th><th>源码职责</th><th>测试位置</th><th>断言重点</th></tr></thead><tbody><tr><td>纯语法和展示</td><td>alias、command token、参数 parser、callback data、help/status/error 文案、card builder</td><td><code>unit/bridge/command/</code></td><td>输入输出、非法输入、文案和 card snapshot；不初始化 bridge context</td></tr><tr><td>单命令 use-case</td><td><code>/runtime</code>、<code>/provider</code>、<code>/new</code>、<code>/his</code>、<code>/auto</code>、<code>/tmux</code> 等命令处理器</td><td><code>unit/bridge/command/commands/</code> 或同名单测</td><td>store 读写、返回 <code>CommandResult</code>、副作用 plan；使用 fake store，不发 IM</td></tr><tr><td>命令组 workflow</td><td>session 命令组、runtime 设置组、history 组、automation 组、terminal/tmux 组</td><td><code>workflow/bridge/command/&lt;group&gt;/</code></td><td>多个命令连续执行后的 session/config/history 状态；使用 fake adapter/store/provider</td></tr><tr><td>daemon mock e2e</td><td>inbound command 经 host dispatch 到 delivery/audit/pin/afterDelivery</td><td><code>e2e/mock-app/bridge/command/</code></td><td>真实 dispatch 入口、reply、audit、card pin、default target 和 scoped binding</td></tr><tr><td>真实飞书故事</td><td>跨群 <code>/new</code>、<code>/cd</code>、<code>/current</code>、<code>/his</code>、runtime/provider 切换、表单和文件</td><td><code>scripts/real-feishu-e2e.ts</code> 场景</td><td>真实 <code>reply_to</code>、真实卡片/文件、群清理、provider 输出路径 gate</td></tr></tbody></table><p>拆分顺序应从最大、最混杂的文件开始：先把 <code>session-thread.ts</code> 中的 list/card/history presentation 和 session use-case 分开；再把 <code>runtime-settings.ts</code> 中纯 formatter、bootstrap helper 和设置命令处理器分开；然后把 <code>tmux.ts</code>/<code>pty.ts</code>/<code>shell.ts</code> 的 parser、screen monitor、runner 和 command handler 边界收窄；最后收缩 <code>dispatch.ts</code>。<code>dispatch</code> 不一定要一次性变成注册机制，但应逐步只保留危险输入校验、命令路由、context 构造、<code>CommandResult</code> delivery 和审计。这样单测可以覆盖 parser/presentation/use-case，workflow 覆盖命令组状态闭环，mock e2e 覆盖 bridge 集成，真实飞书只覆盖完整用户故事。</p><p>当前源码重构采用模块边界优先的完成标准：<code>bridge/host</code> 是生产代码里唯一直接调用 <code>bridge/command</code> 的上层 owner；<code>channels</code>、<code>bridge/turn</code>、<code>runtime</code>、<code>storage</code>、<code>operator-ui</code> 不直接依赖 command；command 也不反向 import <code>bridge/host/*</code>。跨 turn 和 command 共用的 agent question callback 协议位于 <code>src/bridge/callbacks/agent-question.ts</code>，聊天绑定 router 位于 <code>src/bridge/session/channel-router.ts</code>，startup notice target 位于 <code>src/bridge/startup-notice-target.ts</code>。后续如果继续拆 <code>dispatch.ts</code>，应在这个单向依赖边界内演进，而不是为了目录整齐继续细拆每条 slash command。</p><h2 id="现有测试迁移切片" tabindex="-1">现有测试迁移切片 <a class="header-anchor" href="#现有测试迁移切片" aria-label="Permalink to &quot;现有测试迁移切片&quot;">​</a></h2><p>迁移时按切片移动，不要机械搬空 <code>src/__tests__</code>。</p><table tabindex="0"><thead><tr><th>当前测试</th><th>目标切片</th></tr></thead><tbody><tr><td><code>turn-classifier.test.ts</code>、<code>turn-coordinator.test.ts</code>、<code>stream-state.test.ts</code>、<code>response-assembler.test.ts</code></td><td><code>unit/bridge/turn/</code></td></tr><tr><td><code>interactive-turn-*.test.ts</code>、<code>delivery-pipeline.test.ts</code></td><td><code>workflow/bridge/interactive-turn/</code>，其中纯 controller 可拆到 <code>unit/bridge/turn/interactive/</code></td></tr><tr><td><code>mirror-*.test.ts</code></td><td><code>unit/bridge/mirror/</code> 和 <code>workflow/bridge/mirror/</code></td></tr><tr><td><code>command-dispatch.test.ts</code>、<code>help-command.test.ts</code></td><td><code>unit/bridge/command/</code> + <code>workflow/bridge/command/</code></td></tr><tr><td><code>bridge-command-e2e.test.ts</code>、<code>bridge-manager.test.ts</code>、<code>bridge-adapter-runtime.test.ts</code></td><td><code>e2e/mock-app/bridge/</code>，拆出可单测的小规则</td></tr><tr><td><code>codex-*.test.ts</code>、<code>claude-*.test.ts</code>、<code>sse-stream-decoder.test.ts</code></td><td><code>unit/runtime/</code> 和 <code>workflow/runtime/</code></td></tr><tr><td><code>real-codex-*.e2e.test.ts</code>、<code>real-claude-pty-provider.e2e.test.ts</code></td><td><code>e2e/local-process/codex/</code>、<code>e2e/local-process/claude/</code></td></tr><tr><td><code>feishu-adapter*.test.ts</code>、<code>feishu-markdown.test.ts</code>、<code>plain-markdown.test.ts</code>、<code>outbound-artifacts.test.ts</code></td><td><code>unit/channels/feishu/</code>、<code>workflow/channels/delivery/</code></td></tr><tr><td><code>ui-*.test.ts</code></td><td><code>unit/operator-ui/routes/</code> 或 <code>workflow/operator-ui/</code></td></tr><tr><td><code>config.test.ts</code>、<code>runtime-options.test.ts</code>、<code>json-schemas.test.ts</code></td><td><code>unit/configuration/</code></td></tr><tr><td><code>store.test.ts</code>、<code>storage-migrations.test.ts</code>、<code>channel-chat-migration-script.test.ts</code></td><td><code>unit/storage/</code> 和 <code>workflow/storage/migrations/</code></td></tr><tr><td><code>service-manager.test.ts</code>、<code>hot-update-script.test.ts</code></td><td><code>unit/local-service/</code> 和 <code>workflow/local-service/</code></td></tr><tr><td><code>real-feishu-e2e-harness.test.ts</code>、<code>real-e2e-dump.test.ts</code></td><td><code>harness/real-feishu/</code></td></tr></tbody></table><p><code>test-environment-isolation.test.ts</code> 应保留为全局 guard，目标位置是 <code>setup/isolated-home.test.ts</code> 或 <code>harness/test-environment-isolation.test.ts</code>。它必须继续证明测试默认不会读写真实 <code>~/.codex-to-im</code>、<code>~/.codex</code>、<code>~/.claude</code>。</p><h2 id="删除和降级规则" tabindex="-1">删除和降级规则 <a class="header-anchor" href="#删除和降级规则" aria-label="Permalink to &quot;删除和降级规则&quot;">​</a></h2><p>重建时应主动删除冗余测试，而不是只移动文件。</p><p>可删除：</p><ul><li>同一输入输出已经由更低层单测完整覆盖，且高层测试只重复断言同一个字符串。</li><li>旧目录、旧平台或已删除 facade 的兼容性测试。</li><li>只断言 mock 被调用一次、但没有检查状态变化或用户可见结果的测试。</li><li>与真实飞书 harness metadata 不一致的旧场景快照。</li></ul><p>应降级或拆分：</p><ul><li>一个大型 workflow 里混入多个纯 formatter/parser 断言，应把纯逻辑拆到 <code>unit</code>。</li><li>一个 mock-only 测试声称证明完整用户故事，应改名为 <code>workflow</code> 或 <code>mock-app e2e</code>，并在覆盖审计中标明不能替代真实飞书。</li><li>一个真实进程 e2e 只用了 fake executable，没有启动真实 provider 边界，应降级到 <code>workflow/runtime</code>。</li></ul><p>必须保留或补强：</p><ul><li>覆盖隔离 home、真实进程清理、tmux 清理、created chat 清理和 report gate 的测试。</li><li>覆盖 provider 输出路径的测试：SDK 应证明直接 <code>im:</code>，pty/tmux 应证明 <code>mirror:</code>，并检查最终文本不重复。</li><li>覆盖飞书文件、交互卡片、Markdown 原始内容和 <code>reply_to</code> 的真实平台证据。</li></ul><h2 id="命名和运行入口" tabindex="-1">命名和运行入口 <a class="header-anchor" href="#命名和运行入口" aria-label="Permalink to &quot;命名和运行入口&quot;">​</a></h2><p>命名建议：</p><ul><li>单测：<code>*.unit.test.ts</code>，也可以在迁移期保留 <code>*.test.ts</code>。</li><li>本地 workflow：<code>*.workflow.test.ts</code>。</li><li>本地 mock app e2e：<code>*.mock-e2e.test.ts</code>。</li><li>本地真实进程 e2e：<code>*.local-e2e.test.ts</code>。</li><li>真实飞书 harness 自测：<code>*.harness.test.ts</code>。</li></ul><p>未来 <code>scripts/run-tests.js</code> 应从固定读取 <code>src/__tests__/*.test.ts</code> 改为递归发现，并支持按层级过滤：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --unit</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --workflow</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --mock-e2e</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --local-e2e</span></span></code></pre></div><p>真实飞书继续使用：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> real:feishu:e2e</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --list-scenarios</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">CTI_REAL_FEISHU_E2E</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">1</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> real:feishu:e2e</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --scenario</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> basic-dialogue-suite</span></span></code></pre></div><h2 id="迁移顺序" tabindex="-1">迁移顺序 <a class="header-anchor" href="#迁移顺序" aria-label="Permalink to &quot;迁移顺序&quot;">​</a></h2><ol><li>先引入递归 test discovery 和 <code>setup/</code>，保持现有测试全部可运行。</li><li>迁移公共工具：拆出 isolated home、recording adapter、fake provider、runtime proxy 和 assertions。</li><li>迁移纯逻辑单测：<code>shared</code>、<code>domain</code>、<code>configuration</code>、<code>storage</code>、小型 bridge controllers。</li><li>迁移 workflow：command、session、turn、mirror、delivery、UI route。</li><li>迁移本地 e2e：mock app bridge、real Codex/Claude/tmux provider。</li><li>清理冗余测试，并更新 <code>coverage-audit.md</code> 中的本地覆盖和真实飞书证据。</li></ol><p>每一步都必须保持 <code>npm test</code>、<code>npm run typecheck</code> 和 <code>npm run docs:build</code> 可作为阶段验证。涉及真实飞书矩阵或场景 metadata 时，还要检查 <code>npm run real:feishu:e2e -- --list-scenarios</code> 输出与文档一致。</p>`,53)])])}const m=t(o,[["render",n]]);export{u as __pageData,m as default};
