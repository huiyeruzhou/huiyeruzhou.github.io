import{_ as i,o as e,c as o,a2 as t}from"./chunks/framework.BZO4qERu.js";const h=JSON.parse('{"title":"平台配置指南","description":"","frontmatter":{},"headers":[],"relativePath":"guide/platform-setup.md","filePath":"guide/platform-setup.md","lastUpdated":1780478520000}'),a={name:"guide/platform-setup.md"};function n(l,s,r,d,c,p){return e(),o("div",null,[...s[0]||(s[0]=[t(`<h1 id="平台配置指南" tabindex="-1">平台配置指南 <a class="header-anchor" href="#平台配置指南" aria-label="Permalink to &quot;平台配置指南&quot;">​</a></h1><p>本文提供飞书 / Lark 平台的逐步配置说明，会被 <code>setup</code> 和 <code>reconfigure</code> 子命令引用。</p><hr><h2 id="飞书-lark" tabindex="-1">飞书 / Lark <a class="header-anchor" href="#飞书-lark" aria-label="Permalink to &quot;飞书 / Lark&quot;">​</a></h2><h3 id="应用-id-和应用密钥" tabindex="-1">应用 ID 和应用密钥 <a class="header-anchor" href="#应用-id-和应用密钥" aria-label="Permalink to &quot;应用 ID 和应用密钥&quot;">​</a></h3><p><strong>创建飞书/Lark 应用并获取凭据：</strong></p><ol><li>打开飞书开发者后台：<code>https://open.feishu.cn/app</code>，或 Lark 后台：<code>https://open.larksuite.com/app</code>。</li><li>点击 <strong>创建企业自建应用</strong>。</li><li>填写应用名称和描述，点击 <strong>创建</strong>。</li><li>在应用的 <strong>凭证与基础信息</strong> 页面找到： <ul><li><strong>App ID</strong>，格式类似 <code>cli_xxxxxxxxxx</code>。</li><li><strong>App Secret</strong>，点击显示后可看到类似 <code>xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx</code> 的值。</li></ul></li></ol><h3 id="阶段-1-权限和机器人能力" tabindex="-1">阶段 1：权限和机器人能力 <a class="header-anchor" href="#阶段-1-权限和机器人能力" aria-label="Permalink to &quot;阶段 1：权限和机器人能力&quot;">​</a></h3><blockquote><p>先完成阶段 1 并发布版本，再进入阶段 2。飞书要求已发布版本后权限才会生效；bridge 服务也需要有效权限才能建立 WebSocket 连接。</p></blockquote><p><strong>步骤 A：批量添加必需权限</strong></p><ol><li>在应用页面进入 <strong>权限管理</strong>。</li><li>使用 <strong>批量配置</strong>（点击 <strong>切换至按依赖批量配置</strong>，或找到 JSON 编辑器）。</li><li>粘贴下面的 JSON。这些权限是流式卡片和交互按钮所需权限：</li></ol><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;scopes&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;tenant&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      &quot;im:message:send_as_bot&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      &quot;im:message:readonly&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      &quot;im:message.p2p_msg:readonly&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      &quot;im:message.group_at_msg:readonly&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      &quot;im:message:update&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      &quot;im:message.reactions:read&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      &quot;im:message.reactions:write_only&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      &quot;docs:document.comment:create&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      &quot;docs:document.comment:write_only&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      &quot;im:chat:read&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      &quot;im:resource&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      &quot;cardkit:card:write&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      &quot;cardkit:card:read&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ],</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;user&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: []</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><ol start="4"><li>点击 <strong>保存</strong> 应用所有权限。</li></ol><p>如果后台没有批量导入入口，可以在搜索框里逐个添加上述 scope。</p><blockquote><p><strong>重要：</strong> 如果缺少 <code>cardkit:card:write</code>，即使在本地工作台开启飞书流式响应卡片，也无法正常工作。bridge 会记录飞书错误 <code>99991672</code>，并降级为普通最终结果消息。</p></blockquote><blockquote><p><strong>云文档评论：</strong> <code>drive.notice.comment_add_v1</code> 只负责让 bridge 收到评论事件；回复云文档评论和提前添加 <code>Typing</code> reaction 还需要文档评论写权限。如果缺少 <code>docs:document.comment:create</code> 或 <code>docs:document.comment:write_only</code>，飞书会返回 <code>99991672</code>。</p></blockquote><p><strong>步骤 B：启用机器人</strong></p><ol><li>进入 <strong>添加应用能力</strong>，启用 <strong>机器人</strong>。</li><li>设置机器人名称和描述。</li></ol><p><strong>步骤 C：第一次发布，让权限和机器人生效</strong></p><ol><li>进入 <strong>版本管理与发布</strong>，点击 <strong>创建版本</strong>。</li><li>填写版本号 <code>1.0.0</code> 和描述，点击 <strong>保存</strong>，再点击 <strong>提交审核</strong>。</li><li>管理员在 <strong>飞书管理后台</strong> 的 <strong>应用审核</strong> 中审批；如果你是管理员，可以自行审批。</li></ol><p><strong>版本审批通过前，机器人不会工作。</strong></p><h3 id="阶段-2-事件订阅-需要-bridge-已运行" tabindex="-1">阶段 2：事件订阅（需要 bridge 已运行） <a class="header-anchor" href="#阶段-2-事件订阅-需要-bridge-已运行" aria-label="Permalink to &quot;阶段 2：事件订阅（需要 bridge 已运行）&quot;">​</a></h3><blockquote><p>配置事件前必须先启动 bridge 服务。保存事件订阅时，飞书会检查 WebSocket 连接；如果 bridge 未运行，会出现“未检测到应用连接信息”的错误。</p></blockquote><p><strong>步骤 D：启动 bridge 服务</strong></p><p>从本地 <code>codex-to-im</code> 工作台启动 bridge。这会建立飞书需要检测的 WebSocket 长连接。</p><p><strong>步骤 E：配置事件与回调（长连接）</strong></p><ol><li>在左侧边栏进入 <strong>事件与回调</strong>。</li><li>在 <strong>事件订阅方式</strong> 中选择 <strong>长连接</strong>（WebSocket 模式）。</li><li>点击 <strong>添加事件</strong> 并添加： <ul><li><code>im.message.receive_v1</code>：接收消息。</li><li><code>drive.notice.comment_add_v1</code>：接收飞书云文档评论。</li><li><code>im.chat.member.bot.deleted_v1</code>：检测 bot 被移出群聊。</li><li><code>im.chat.disbanded_v1</code>：检测群聊被解散。</li></ul></li><li>点击 <strong>添加回调</strong> 并添加： <ul><li><code>card.action.trigger</code>：卡片交互回调，用于权限审批按钮。</li></ul></li><li>点击 <strong>保存</strong>。</li></ol><p><strong>步骤 F：第二次发布，让事件订阅生效</strong></p><ol><li>进入 <strong>版本管理与发布</strong>，点击 <strong>创建版本</strong>。</li><li>填写版本号 <code>1.1.0</code>，点击 <strong>保存</strong>，再点击 <strong>提交审核</strong>，由管理员审批。</li><li>审批通过后，bot 即可接收和回复消息。</li></ol><blockquote><p><strong>持续规则：</strong> 任何权限、事件或能力变更，都需要重新发布版本并由管理员审批。</p></blockquote><h3 id="从旧版本升级" tabindex="-1">从旧版本升级 <a class="header-anchor" href="#从旧版本升级" aria-label="Permalink to &quot;从旧版本升级&quot;">​</a></h3><p>如果你已经配置过飞书应用，需要执行以下步骤：</p><ol><li><strong>添加新权限</strong>：进入权限管理并添加这些 scope： <ul><li><code>cardkit:card:write</code>、<code>cardkit:card:read</code>：流式卡片。</li><li><code>im:message:update</code>：实时更新卡片内容。</li><li><code>im:message.reactions:read</code>、<code>im:message.reactions:write_only</code>：Typing 指示。</li><li><code>docs:document.comment:create</code>、<code>docs:document.comment:write_only</code>：云文档评论回复和 Typing reaction。</li></ul></li><li><strong>发布新版本</strong>：权限变更只有在新版本审批通过后才会生效。</li><li><strong>启动或重启 bridge</strong>：从本地 <code>codex-to-im</code> 工作台启动，让 WebSocket 连接处于活跃状态。</li><li><strong>添加回调</strong>：进入事件与回调，添加 <code>card.action.trigger</code> 回调，用于权限按钮卡片交互。保存时飞书会校验 WebSocket 连接，因此这一步要求 bridge 正在运行。</li><li><strong>再次发布</strong>：新增回调也需要发布新版本并由管理员审批。</li><li><strong>重启 bridge</strong>：在本地 <code>codex-to-im</code> 工作台停止并重新启动 bridge，让新能力生效。</li></ol><h3 id="当前-codex-runtime-的飞书流式行为" tabindex="-1">当前 Codex runtime 的飞书流式行为 <a class="header-anchor" href="#当前-codex-runtime-的飞书流式行为" aria-label="Permalink to &quot;当前 Codex runtime 的飞书流式行为&quot;">​</a></h3><p>即使飞书权限配置正确，当前 <code>codex</code> runtime 也不保证把 token-by-token 文本流式写入飞书卡片。</p><p>截至 <strong>2026-03-24</strong>，<code>Codex CLI / SDK</code> 事件流通常会在 <code>agent_message</code> item 完成时才发出 assistant 文本，而不是持续发出 token delta。实际使用中，飞书流式卡片更应理解为：</p><ul><li>提前展示 <code>Thinking</code> / 工具进度更新。</li><li>在完成时把最终回复文本写入卡片。</li></ul><p>因此，如果卡片已经成功创建，但最终答案是在完成后一口气出现，这属于当前 <code>codex</code> runtime 的预期行为。</p><h3 id="域名-可选" tabindex="-1">域名（可选） <a class="header-anchor" href="#域名-可选" aria-label="Permalink to &quot;域名（可选）&quot;">​</a></h3><p>默认值：<code>https://open.feishu.cn</code>。</p><p>Lark 国际版使用 <code>https://open.larksuite.com</code>。</p><p>留空则使用默认飞书域名。</p><h3 id="允许用户-id-可选" tabindex="-1">允许用户 ID（可选） <a class="header-anchor" href="#允许用户-id-可选" aria-label="Permalink to &quot;允许用户 ID（可选）&quot;">​</a></h3><p>飞书用户 ID 使用 open_id 格式，例如 <code>ou_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx</code>。</p><p>可以在飞书管理后台的用户资料中找到。</p><p>留空表示允许所有能给 bot 发消息的用户使用。</p>`,46)])])}const k=i(a,[["render",n]]);export{h as __pageData,k as default};
